var count=function(arr){
  return "testing " +arr.length;
};
var adder = function(a,b){
  return `the sum of 2 numver is ${a+b}`
}
var pi= Math.PI;

module.exports = {
  count:count,
  adder:adder,
  pi:pi
};
